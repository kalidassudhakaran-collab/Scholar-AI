# Optional AI packages (install after requirements-local.txt)
# CPU PyTorch first on Windows:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

transformers>=4.38
# Note: v5 removed summarization pipeline — code uses AutoModelForSeq2SeqLM directly
sentencepiece>=0.2
sacremoses>=0.1
protobuf>=4.0
pytesseract>=0.3
pdfplumber>=0.11
python-docx>=1.1
openai-whisper>=20231117
yt-dlp>=2024.1
youtube-transcript-api>=0.6
# Optional: semantic plagiarism (otherwise lexical fallback is used)
sentence-transformers>=2.2
# AI-generated text detection (otherwise heuristic fallback is used)
# Model: Hello-SimpleAI/chatgpt-detector-roberta (auto-download on first use)
